import React from 'react';
import { Text, Box } from 'ink';
// Add command description for help output
export const description = "Execute various Ethereum actions and transactions";
export default function Action() {
    return (React.createElement(Box, { flexDirection: "column", padding: 1 },
        React.createElement(Text, { bold: true }, "TEVM Action Commands"),
        React.createElement(Text, null, "Run `tevm action --help` to see available action subcommands."),
        React.createElement(Box, { marginY: 1 },
            React.createElement(Text, null, "Available actions include: simulateCalls, sendRawTransaction, createAccessList, getBalance"))));
}
