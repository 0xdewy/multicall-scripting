import React from 'react';
import { Text, Box } from 'ink';
const ActionTab = ({ actionName, interactive, rpcUrl, ActionComponent }) => {
    if (!interactive) {
        return (React.createElement(Box, { flexDirection: "column" },
            React.createElement(Text, { bold: true },
                actionName,
                " Action"),
            React.createElement(Text, null, "Press Enter to use this action interactively")));
    }
    // Only render the actual action component when in interactive mode
    return (React.createElement(ActionComponent, { options: {
            interactive: true,
            rpc: rpcUrl,
            formatJson: true
        }, args: [] }));
};
export default ActionTab;
