# Ticket H1 — Gas: cost is in the bytes, not the dispatch

**Claim:** Dominant interpreter-attributable gas = memory expansion + calldata/returndata
copying between sub-calls, not opcode dispatch; `mcopy`/memory-layout is where savings live.

**What would settle it:** corpus evidence on where EVM gas concentrates (superoptimizer
targeting memory/storage; gas-analysis frameworks; out-of-gas/memory papers), and whether any
*direct* interpreter-vs-native measurement exists.

**Status:** unclaimed → proponent + falsifier + rebuttal in 04-evidence/.
